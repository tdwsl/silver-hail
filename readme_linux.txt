To run this game on Linux, you will need SDL2 and SDL2_mixer installed.

To do this on Ubuntu 21.04, you could use the following command:
"sudo apt update && sudo apt install libsdl2-2.0-0 libsdl2-mixer-2.0-0 -y"

For other architectures or operating systems, see README.md for tips on
compiling.
